# 挑战赛测试用例文档

## 概述

本文档详细说明了挑战赛系统的完整测试用例，涵盖了所有时间段、奖励机制、边界条件和异常情况的测试场景。

## 挑战赛业务逻辑概述

### 时间段说明
挑战赛有4个关键时间段：
- **OpenTime**: 比赛开始时间（玩家可以参加比赛）
- **CloseTime**: 比赛关闭时间（客户端判断不能进入游戏，但仍可提交成绩）
- **EndTime**: 比赛结束时间（不能再提交成绩）
- **OverTime**: 比赛结算时间（过后奖励变成邮件发送）

### 奖励机制
1. **积分奖励**: 根据成绩达到不同条件获得（低分、中分、高分）
2. **排位奖励**: 根据最终排名获得
3. **邮件奖励**: 结算时间后未领取的奖励通过邮件发送

### 主要接口
- `GetChallenge`: 获取挑战赛列表
- `JoinChallenge`: 参加挑战赛
- `GainChallengeReward`: 获得挑战赛奖励

## 测试用例结构

### 1. 综合时间段测试 (TestChallengeComprehensive)

测试挑战赛在不同时间段的行为表现：

#### 1.1 比赛开始前
- **预期行为**: 无法参加，但可以在GetChallenge中看到（作为下一场比赛）
- **测试要点**:
  - GetChallenge应该返回未来的比赛
  - JoinChallenge应该返回错误码4（"挑战赛未开放或已结束"）

#### 1.2 比赛开始时
- **预期行为**: 可以参加，可以提交成绩
- **测试要点**:
  - JoinChallenge应该成功
  - 可以提交成绩
  - GetChallenge应该显示已参加的比赛

#### 1.3 比赛进行中
- **预期行为**: 可以参加，可以提交成绩
- **测试要点**:
  - 正常参赛流程
  - 成绩提交功能
  - 竞标赛分配逻辑

#### 1.4 比赛关闭时
- **预期行为**: 客户端判断不能进入游戏，但还可以提交成绩
- **测试要点**:
  - JoinChallenge仍然可以成功
  - 成绩提交仍然有效
  - 客户端通过CloseTime判断不能进入游戏

#### 1.5 比赛结束时
- **预期行为**: 无法参加，无法提交成绩，可以手动领取奖励
- **测试要点**:
  - JoinChallenge应该失败
  - 无法提交新成绩
  - GainChallengeReward应该可以领取奖励

#### 1.6 比赛结算前
- **预期行为**: 无法参加，无法提交成绩，可以手动领取奖励
- **测试要点**:
  - 奖励领取功能正常
  - 积分奖励和排位奖励都可以领取

#### 1.7 比赛结算后
- **预期行为**: 无法参加，无法提交成绩，奖励变成邮件发送
- **测试要点**:
  - 自动邮件发送功能
  - GetChallenge应该返回GetReward=true
  - 邮件内容正确性

### 2. 奖励场景测试 (TestChallengeRewardScenarios)

测试不同成绩和排名情况下的奖励机制：

#### 2.1 无成绩玩家
- **场景**: 玩家参加但没有提交成绩
- **预期**: 不应该获得任何奖励
- **测试要点**: GainChallengeReward返回错误码5（"没有提交成绩"）

#### 2.2 低分玩家 (score >= Condition01)
- **场景**: 玩家达到低分条件
- **预期**: 获得低分奖励和排位奖励
- **测试要点**: 奖励数量和类型正确

#### 2.3 中分玩家 (score >= Condition02)
- **场景**: 玩家达到中分条件
- **预期**: 获得低分、中分奖励和排位奖励
- **测试要点**: 累积奖励逻辑正确

#### 2.4 高分玩家 (score >= Condition03)
- **场景**: 玩家达到高分条件
- **预期**: 获得所有积分奖励和排位奖励
- **测试要点**: 完整奖励发放

#### 2.5 边界分数玩家
- **场景**: 玩家分数刚好达到某个条件边界
- **预期**: 边界条件处理正确
- **测试要点**: 等于边界值时的奖励发放

#### 2.6 高分低排名玩家
- **场景**: 玩家分数高但排名相对较低
- **预期**: 获得所有积分奖励和对应排位奖励
- **测试要点**: 积分奖励和排位奖励独立计算

### 3. 边界条件测试 (TestChallengeBoundaryConditions)

测试各种边界情况和异常处理：

#### 3.1 重复参加挑战赛
- **测试要点**: 
  - 第一次参加成功
  - 第二次参加失败，返回错误码2（"挑战赛不可重复参加"）

#### 3.2 竞标赛满员情况
- **测试要点**:
  - 竞标赛达到MaxPart时创建新竞标赛
  - 新玩家能够成功加入新竞标赛
  - 批次编号正确递增

#### 3.3 重复领取奖励
- **测试要点**:
  - 第一次领取成功
  - 第二次领取失败，返回相应错误码

#### 3.4 时间临界点
- **测试要点**:
  - 各个时间点的精确处理
  - 时间边界的准确判断

#### 3.5 无效挑战赛ID
- **测试要点**:
  - 使用不存在的挑战赛ID
  - 返回错误码1（"挑战赛Id不存在"）

#### 3.6 邮件奖励发送
- **测试要点**:
  - 过期奖励自动变成邮件
  - 邮件内容和格式正确

### 4. 排名场景测试 (TestChallengeRankingScenarios)

测试不同排名情况下的奖励分配：

#### 4.1 基础排名测试
- **测试要点**:
  - 基本排名计算正确
  - 排名奖励分配准确

#### 4.2 相同分数排名测试
- **测试要点**:
  - 相同分数玩家排名处理
  - 并列排名的奖励分配

#### 4.3 大规模排名测试
- **测试要点**:
  - 大量玩家的排名计算性能
  - 排名奖励分配的正确性

### 5. 完整工作流程测试 (TestChallengeCompleteWorkflow)

测试完整的业务流程：

#### 5.1 正常参赛流程
- **流程**: GetChallenge → JoinChallenge → SubmitScore → GetChallenge → WaitForEnd → GainReward
- **测试要点**: 完整流程无异常

#### 5.2 迟到参赛流程
- **流程**: 比赛快结束时参加
- **测试要点**: 临近结束时参赛的处理

#### 5.3 错过手动领奖流程
- **流程**: 玩家错过手动领奖，奖励变成邮件
- **测试要点**: 邮件补发机制

#### 5.4 只参加不提交成绩流程
- **流程**: 参加但不提交成绩
- **测试要点**: 无成绩时的处理

## 测试执行方法

### 环境准备
1. 确保Go环境已安装
2. 确保项目依赖已安装（`go mod tidy`）
3. 确保测试数据库和缓存可用

### 执行测试

#### 方法1：使用批处理脚本
```batch
run_challenge_tests.bat
```

#### 方法2：手动执行
```bash
# 执行所有测试
go test -v ./server -run TestChallenge -timeout=30m

# 执行特定测试
go test -v -run TestChallengeComprehensive ./server -timeout=30m
go test -v -run TestChallengeRewardScenarios ./server -timeout=30m
go test -v -run TestChallengeBoundaryConditions ./server -timeout=30m
go test -v -run TestChallengeRankingScenarios ./server -timeout=30m
go test -v -run TestChallengeCompleteWorkflow ./server -timeout=30m
```

#### 方法3：执行具体实现测试
```bash
go test -v -run TestChallengeTimeScenarioImpl ./server -timeout=30m
go test -v -run TestChallengeRewardScenarioImpl ./server -timeout=30m
go test -v -run TestChallengeBoundaryConditionsImpl ./server -timeout=30m
go test -v -run TestChallengeTimeBoundaryImpl ./server -timeout=30m
```

### 测试报告
执行测试后，会生成详细的测试报告，包括：
- 每个测试场景的执行结果
- 失败测试的详细错误信息
- 测试覆盖率报告
- 性能测试结果

## 测试数据配置

### 挑战赛模板配置
```go
challengeTemplate := &template.TplChallenge{
    ID:           1001,
    Name:         "测试挑战赛",
    ActivityID:   "test_activity_001",
    OpenTime:     "2024-01-01 10:00:00",
    CloseTime:    "2024-01-01 11:00:00",
    EndTime:      "2024-01-01 12:00:00",
    RewardRemains: 60, // 1小时奖励保留时间
    MaxPart:      100,
}
```

### 活动模板配置
```go
activityTemplate := &template.TplActivityInfo{
    ID:          "test_activity_001",
    Name:        "测试活动",
    Condition01: 1000,  // 低分条件
    Condition02: 5000,  // 中分条件
    Condition03: 10000, // 高分条件
    Reward01:    "low_score_reward",
    Reward02:    "mid_score_reward",
    Reward03:    "high_score_reward",
}
```

## 预期测试结果

### 成功标准
- 所有时间段测试通过
- 所有奖励机制测试通过
- 所有边界条件测试通过
- 所有排名场景测试通过
- 所有完整工作流程测试通过

### 失败处理
如果测试失败，请检查：
1. 数据库连接是否正常
2. 模板配置是否正确
3. 时间计算逻辑是否正确
4. 奖励发放逻辑是否正确
5. 邮件发送功能是否正常

## 性能测试

### 并发测试
- 多个玩家同时参加挑战赛
- 竞标赛满员时的并发处理
- 大量玩家同时领取奖励

### 压力测试
- 大规模玩家参与
- 长时间运行测试
- 内存和CPU使用监控

## 测试维护

### 新增测试场景
1. 在相应的测试文件中添加新的测试函数
2. 更新测试执行脚本
3. 更新本文档

### 修改测试数据
1. 修改模板配置
2. 更新预期结果
3. 重新执行测试验证

## 常见问题

### Q1: 测试执行时间过长
A1: 可以使用`-timeout`参数调整超时时间，或者分别执行各个测试组

### Q2: 测试数据库连接失败
A2: 检查数据库配置，确保测试数据库可用

### Q3: 时间相关测试失败
A3: 检查时间模拟逻辑，确保时间设置正确

### Q4: 奖励测试失败
A4: 检查奖励模板配置，确保奖励ID存在

## 结论

本测试用例集合全面覆盖了挑战赛系统的各种场景，包括正常流程、异常情况、边界条件和性能测试。通过执行这些测试，可以确保挑战赛系统的稳定性和可靠性。

建议在每次代码修改后都运行完整的测试套件，确保修改不会破坏现有功能。同时，随着业务需求的变化，应及时更新和扩展测试用例。 